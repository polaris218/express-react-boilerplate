const muiStyle = theme => ({
    main: {
        marginTop: theme.spacing.unit * 3,
        border: '1px',
        borderColor: '#ff0000',
        borderStyle: 'solid',
    }
})

export default muiStyle;